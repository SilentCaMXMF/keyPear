{
  "apiVersion": 1,
  "providers": [
    {
      "name": "keyPear Dashboards",
      "folder": "keyPear",
      "type": "file",
      "options": {
        "path": "/etc/grafana/provisioning/dashboards"
      }
    }
  ]
}
